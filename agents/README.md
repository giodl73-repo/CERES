# agents/

Chronicle-pattern agent definitions used across CERES plans. Each agent file
specifies the role, instructions, tools, and conventions for a Claude instance
that performs a recurring research, authoring, or validation task.

Populated across plans as each agent type is first needed.
